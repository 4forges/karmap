env:
  matrix:
    # Defaults - Ubuntu 16.04.
    - type: defaults
      distro: ubuntu1604
      init: /sbin/init
      run_opts: "--privileged"

language: ruby
rvm:
  - 2.3.3

before_install:
  - gem install bundler -v 1.13.6
